// import {createAction, props} from '@ngrx/store';
// import { Product } from '../app/page/home/components/product/product.model';

// export const addToCart = createAction('Add to cart', props<{product: Product}>());
// export const deleteFromCart = createAction('Delete from cart', props<{id: string}>());
// export const updateCartItem = createAction('Reset cart',props<{id:string, quantity:number}>());